#include <iostream>

int main(int argc, char* argv[]) {
    (void)argc;
    (void)argv;
    // Placeholder implementation. Returns an empty JSON array so callers can
    // fall back to Java/Python scanning until the native Windows decoder is added.
    std::cout << "[]" << std::endl;
    return 0;
}
